# templates/usage-metrics

The ServiceBay deploy artifact for this repo's app — not yet authored.

Per `docs/TEMPLATE_AUTHORING.md` (in `mdopp/servicebay`) and `templates/CLAUDE.md`
there, a template needs: `template.yml` (Pod YAML, mandatory annotations —
`servicebay.label`, `servicebay.ports`, `servicebay.schema-version`,
`servicebay.healthcheck`), `variables.json`, and this `README.md` filled in.

Tracked as an initial issue in this repo — see the repo's issue tracker.
